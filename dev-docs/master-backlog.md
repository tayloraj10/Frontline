# Master Backlog — 2026-08-07

Single source of truth for "what's left, across everything." Each item links to the dev doc with full context/detail — update that doc when working the item, then reflect status here. Pure how-to/setup guides (no backlog of their own) are noted at the bottom for reference, not repeated as tasks.

## Working order (set 2026-08-11, work one at a time)

1. [x] QR code generation anywhere there's a share button — done 2026-08-12
2. [x] Partner businesses: multi-location support — done 2026-08-12
3. [x] Business "radius of influence" view + Business user portal (redemption-history detail) — done 2026-08-13
4. [ ] Convert multi-field modals to step-by-step wizard flow
5. [ ] SEO improvements

## Mobile / Capacitor / App Store push

- [ ] **iOS native scaffolding** — `npx cap add ios` never run, no `ios/` directory. Mac-only. → `ios-setup-2026-08-06.md`, `capacitor-scoping-2026-08-04.md`
- [ ] **Apple/Google dev account legal-entity decision** — proceeding on personal accounts for now; blocks physical iOS builds, signed release builds, store submission. → `capacitor-scoping-2026-08-04.md`
- [ ] **Android release keystore** — doesn't exist yet; needed for signing + `assetlinks.json` SHA-256. Until filled, Android App Links to `frontlinemaps.com` can't verify, so both Google OAuth return-to-app **and push notification tap-to-open** fall back to opening Chrome instead of the app (confirmed 2026-08-07 via a real push test on emulator). → `capacitor-scoping-2026-08-04.md`, `new-machine-setup-2026-08-06.md`, `android-release-checklist.md`
- [ ] **Restrict Firebase Android/iOS/Browser API keys** in Google Cloud Console (GitHub flagged `google-services.json` and `GoogleService-Info.plist` keys as exposed secrets 2026-08-07 — not real secrets, but should be restricted to package/bundle ID + SHA-1 anyway). **Deliberately left unrestricted for now (2026-08-09)** — user chose to prioritize finishing Android release prep first, revisit later. Package/bundle ID for both platforms is now `com.frontlinemaps.app` (renamed from `com.frontline.app` 2026-08-08 after discovering the old name was already taken on Play Store). There are currently **two Android keys** in Cloud Console (old `com.frontline.app` app was deleted from Firebase, but its auto-created key didn't disappear the way the old iOS key's did — likely just console cache lag, unconfirmed) — check both when restricting later. Add the release keystore's SHA-1 too once it exists or signed release builds will fail the restriction. → `android-release-checklist.md`, `ios-release-checklist.md`
- [ ] **Full Android/iOS release checklists scoped** (2026-08-07) — keystore/signing, store listing, privacy declarations, submission steps for both platforms, not yet executed. → `android-release-checklist.md`, `ios-release-checklist.md`
- [ ] **Push notifications: prod deploy** — Edge Function not deployed, migration 062 not applied to prod, Vault secrets not armed on prod DB. → `push-notifications-prod-deploy-checklist.md`
- [ ] **Push notifications: Android manual test pass** (backgrounded/killed app, tap-to-open routing) — needs real device/build. → `push-notifications-scoping-2026-08-06.md`
- [ ] **Push notifications: iOS test pass** — blocked on iOS scaffolding above. → `push-notifications-scoping-2026-08-06.md`
- [ ] **Social milestone pushes** (new, scoped 2026-08-07) — notify *other* users when someone hits a milestone, for engagement. Needs scope/volume/fan-out design before any migration work. → `push-notifications-scoping-2026-08-06.md` ("Backlog: social milestone pushes")
- [ ] **Foreground push has no tray banner on Android** (confirmed 2026-08-07, not a bug) — FCM send succeeds but Android suppresses auto-display while app is foregrounded; needs a `pushNotificationReceived` listener + `@capacitor/local-notifications` if a foreground tray banner is wanted. → `push-notifications-scoping-2026-08-06.md`
- [ ] **Milestone ladder headroom** — bag (caps at 100) and contribution-count (caps at 100) ladders top out much sooner than the points ladder (caps at 10000); extend if real usage shows users blowing past them quickly. → `push-notifications-scoping-2026-08-06.md`
- [ ] **Mobile-first design audit pass** — every page/component for layout, touch targets, breakpoints, safe-area handling; scoped as its own pass before/alongside Capacitor wrapping. → `dev-plan-2026-08-03-mobile-first.md` (item 8), `mobile-first-design-system.md`
- [ ] **Full mobile UI polish pass** (noted 2026-08-08, after first iOS on-device test) — current mobile UI is functional but rough (bottom tab bar, general layout); wants a proper visual pass plus animations/transitions once core functionality is stable. Broader than the bug-fix-level tweaks done during iOS bring-up (splash screen, bottom tab bar spacing, map safe-area cutoff). → `ios-setup-2026-08-06.md`
- [ ] **Audit remaining browser APIs for native WebView quirks** (noted 2026-08-08, after the camera fix) — the in-app camera (`getUserMedia`) needed a real native plugin (`@capacitor/camera`) because it silently didn't work in Capacitor's WKWebView; same risk category, not yet verified on-device: (1) `target="_blank"` links to arbitrary external sites/socials (partner website/social links, business "Visit website" popup on the Trash War map) don't route through `@capacitor/browser`'s `Browser.open` the way OAuth already does — Google Maps links happen to work today only because `google.com/maps` is an iOS universal link, not because the app handles it; (2) `navigator.clipboard.writeText()` copy-to-clipboard buttons (redemption code copy, support email copy, admin partner-apply URL copy) have no `@capacitor/clipboard` fallback if the WKWebView clipboard API turns out to be unreliable. → `capacitor-testing-guide.md`
- [ ] **"Get directions" links don't respect iOS preferred-maps-app** (scoped 2026-08-12) — all 3 call sites (`CampaignMap.tsx` business popup, `CleanupEventDetail.tsx`, `PartnerDetailClient.tsx`) hardcode a `google.com/maps/dir` URL. Android already gets a native app-picker for free (App Links), but iOS never offers Apple Maps/Waze/etc. — it just opens Google Maps in-browser or the Google Maps app if installed. Fix: small `getDirectionsUrl(lat, lng)` helper using `isIOS()` (already exists in `lib/share.ts`) to swap in `https://maps.apple.com/?daddr=lat,lng` on iOS, which does respect the user's actual default-maps-app setting; keep the Google URL everywhere else. No new dependency needed. → `capacitor-testing-guide.md` (related: "Audit remaining browser APIs" item above)
- [ ] **Store listings** — screenshots, description copy, privacy policy URL, age rating, permissions justification. Not started. → `capacitor-scoping-2026-08-04.md`, `android-release-checklist.md`, `ios-release-checklist.md`
- [ ] **Store listing content prep (info + screenshots)** (noted 2026-08-09) — need to produce the actual assets/copy for both iOS App Store and Google Play listings so public testing (TestFlight / Internal Testing) and eventual release can go up. Most content should be shareable across both platforms: app name/subtitle, short + full description, keywords, category, support URL, privacy policy URL, marketing/promo text, app icon, feature graphic. Screenshots need platform-specific sizes/device frames (iOS: 6.7"/6.5" iPhone required sizes; Android: phone + optional tablet) but can come from the same in-app flows/shots. Age rating questionnaires and permissions justification text differ per store and need to be done separately. → `android-release-checklist.md`, `ios-release-checklist.md`
- [ ] **Build + submit** — TestFlight (Mac) and Android Internal Testing track, then review submission. Not started. → `capacitor-scoping-2026-08-04.md`, `android-release-checklist.md`, `ios-release-checklist.md`
- [ ] **React Native rewrite** — deferred until after Capacitor version ships. → `dev-plan-2026-08-03-mobile-first.md` (item 10)
- [x] **Drop "US & UK only" from the Trash War campaign card** — done (UI-only hide in `campaigns/page.tsx`, badge just doesn't render for `trash-war`; `geo_scope` data left untouched since the campaign is still genuinely US/UK-only). On `phase2/capacitor-scoping`, ships with that branch.

## Modals & forms UX

- [ ] **HIGH PRIORITY — Campaign "how it works" info modal overflows on mobile** (noted 2026-08-11) — Trash War's info modal still runs off-screen on mobile devices; a prior fix attempt didn't hold. Plan: give these info modals a fixed height per campaign so long content scrolls internally instead of overflowing the viewport.
- [ ] **HIGH PRIORITY — Rework campaign instructions modals to a compact grid layout** (noted 2026-08-11) — particularly Trash War's; move off the current long-scroll layout to a denser grid so more info is visible at a glance on one page.
- [ ] **Convert multi-field modals to step-by-step wizard flow** (noted 2026-08-11) — logging a cleanup and creating a group cleanup event are currently one long form; break into bite-sized steps to reduce missed fields, especially for newer users. Caution: step ordering matters — the "add a photo" step lets users drag the pin anywhere, but most users should leave the pin at their actual current location, so a later/optional step shouldn't make it easy to accidentally relocate the pin for users who shouldn't be moving it. Touches `CreateCleanupEventForm.tsx` and `ContributionPanel.tsx`'s modal, which must be kept in sync manually (see item above on duplicated forms).

## Naming / branding

- [ ] **Rename "Trash War"** — blocked on picking a new name. Once named: display name, nav copy, marketing/legal docs, dev-doc titles, and a call on whether to rename the `trash-war` slug itself (bigger lift, affects URLs + seed data). → `dev-plan-2026-08-03-mobile-first.md` (item 4)

## Admin / roles

- [ ] **HIGH PRIORITY — Multi-tier admin roles** (noted 2026-08-12) — not started, needs design. Two pieces: (1) **scoped permission roles** — e.g. a "business approver" role that can approve/reject partner businesses without getting the full admin suite; need to figure out which other admin actions are worth carving into their own scoped roles beyond that first one; (2) **super admin role** — a role above regular admin that can control everything (including managing other admins' roles), restricted to the user only. Design questions: assignment model (join table vs. column), what "campaign-scoped" vs. "action-scoped" means per tab/permission, settings-change sign-off flow, how this interacts with the existing `admin.py` no-auth-check gap below. → `dev-plan-2026-08-03-mobile-first.md` (item 5)
- [ ] **`admin.py` has no auth/permission checks** on any route — prod-exclusion is the only current protection. Add real admin-role dependency check, then remove the exclusion. → `campaign-app-scope.md`
- [ ] **Flip `LEGAL_GATE_ENABLED` back to `true`** in `LegalGate.tsx` when ready to require re-acceptance (disabled 2026-08-02). Confirm migration `052_legal_acceptance.sql` is applied to prod first. → `dev-backlog-2026-07-24.md` (item 10)
- [ ] **Group applications migration `047_group_applications.sql`** — written but not yet applied to prod. → `campaign-app-scope.md`

## Territory / geo stats

- [ ] **Load `nyc_borough` data into prod DB** — borough stats toggle is shipped in code but non-functional in prod until this lands. → `project_nyc_borough_outline_deferred` memory, `dev-plan-2026-08-03-mobile-first.md` (item 6)
- [ ] **Per-borough stats UI/design** + decide outline coloring (plain gold vs. per-borough match-colored). → `dev-plan-2026-08-03-mobile-first.md` (item 6)
- [ ] **Territory decay cron is a deployed no-op** — `decay_elapsed` condition type never implemented, nothing sets `decay_starts_at`. → `campaign-app-scope.md`
- [ ] **Contested-zone alerts** — needs a margin-to-flip concept (currently only leader's `total_value` is stored, no runner-up), a `zone_contested` trigger, in-app-only notification surface. → `trash-war-feedback-backlog.md` (item 7), `project_trash_war_deferred` memory
- [ ] **Geolocation shared-owner refactor** — unify `CampaignMap`'s `GeolocateControl` + `ContributionPanel`'s independent trigger into one shared hook/store. Deferred, ~1-2 sessions, fully manual QA burden. → `cleanup-events-dev-plan.md` (item 12), `project_geolocation_shared_owner_refactor` memory
- [ ] **Group logo on captured territory** — deferred since territory-capture layer is going off by default; revisit if it becomes first-class again. → `cleanup-events-dev-plan.md` (item 11)
- [ ] **Cleanup pin-picker ZIP-lock is confusing/outdated** — dragging the pin when logging a cleanup is constrained to the user's starting zip/postcode (leftover from territory-claim anti-gaming), and always says "ZIP code" regardless of which geo layer is visible. Client-side only, not server-enforced. Leaning toward replacing with a distance-radius check instead of a geo_unit boundary. → `campaign-app-scope.md`
- [ ] **Geo stats page** (scoped 2026-08-10, not started) — new page (or a toggle/tab on the existing Leaderboard page) for Trash War showing where the action is happening and who's leading. Filterable by **time interval** (this week/this month/all-time) and drillable by **geo level** (overall → borough → neighborhood → ZIP). Each level shows both an aggregate stat breakdown (total contributions, total value, unique contributors/groups) and a leaderboard of top individuals + top groups active there for the selected interval — distinct from the existing `leaderboard_entries` table, which is campaign-wide/all-time only with no geo or time slicing. Source data (`contributions.geo_unit_id`, `submitted_at`) already supports the needed `GROUP BY geo_unit_id, date_trunc(interval, submitted_at)` query; ZIP/neighborhood/borough already exist as `geo_units.unit_type` values via existing seeders. Borough level inherits the existing `nyc_borough` prod-data-load blocker (see item above); ZIP/neighborhood are not blocked. Open questions: new page vs. Leaderboard toggle, exact time intervals, live query vs. a materialized summary table if traffic demands it, tie-breaking rule for narrower time/geo windows. → `campaign-app-scope.md` (schema), `project_nyc_borough_outline_deferred` memory
- [ ] **ZIP code heat map layer** (noted 2026-08-13, not started/needs scoping) — new map layer variant on the existing ZIP code geo layer that fills each ZIP polygon with a color intensity based on activity level (contribution count or value) in that ZIP, instead of the current flat/outline styling. Heat mapping should be scoped to the **current viewport extent** — only ZIPs visible on screen factor into the color scale, so the map recolors as the user pans/zooms rather than using a fixed global scale. Open questions: which activity metric (contribution count vs. total value vs. unique contributors), color scale/legend design, recompute-on-pan performance (likely needs viewport-bounded query against `contributions.geo_unit_id` rather than refetching all ZIPs), time-interval filter or all-time only, interaction with the Geo stats page item above (could share the same aggregation query). → `campaign-app-scope.md` (geo layer code), relates to Geo stats page item above

## Campaign system / events

- [ ] **Event condition types unimplemented**: `decay_elapsed`, `external_api` (schema only), `group_tie` (not even designed). → `campaign-app-scope.md`
- [ ] **Event effect types are stubs**: `notification`, `cascade_unlock`, `seasonal_reset`, `decay_start`. → `campaign-app-scope.md`
- [ ] **Campaign auto-`completed` transition** — not built (on `ends_at` pass or win condition). → `campaign-app-scope.md`
- [ ] **Cleanup Event contribution anti-fraud gap** — no server-side proximity check on `cleanup_event_id` submissions (client-side only). Deliberately deferred; escalation path if needed: flag-not-block → require photo → hard enforcement. → `campaign-app-scope.md`
- [ ] **Campaign Create Form**: `contribution_type` enum is confusing outside original 4-campaign context (decide enum vs. free text); `census_tract` geo unit has no loader/seeder (remove from dropdown or build TIGER loader). → `campaign-app-scope.md`
- [ ] **Campaign 6 (Ground Truth)** — post-launch, not started. → `campaign-app-scope.md`
- [ ] **Campaign 7 (Life Detox, board-game type)** — future, needs new `campaign_type`, board UI, progress tracking. → `campaign-app-scope.md`
- [ ] **Campaign 8 (Full Life)** — future, data model compatible, no contribution form/streak UI yet. → `campaign-app-scope.md`
- [ ] **Solarpunk multi-resolution hex grid** (res-3/res-5 zoom swap) — future, not built. → `campaign-app-scope.md`
- [ ] **Groups: category-tagging UI** — schema exists, UI doesn't. → `campaign-app-scope.md`
- [ ] **Group cleanup events: double-points multiplier never applies** (`apply_multiplier=False` hardcoded) — needs real design (geo-scoping, prorating mid-event, predictability) before changing. → `campaign-app-scope.md`
- [ ] **Partner businesses**: no per-business detail page (fine for now); no expiry/refund flow if a business goes inactive post-redemption (manual admin cleanup only); no push/email on redemption. → `campaign-app-scope.md`
- [x] **QR code generation anywhere there's a share button** (noted 2026-08-11, done 2026-08-12) — added as a reusable addition to the shared `ShareButton.tsx` (new `QRCodeModal.tsx`, `qrcode` npm package for client-side canvas/data-URL generation, no external service call). `ShareButton` now renders a QR icon/link next to its existing share action (both `icon` and `text` variants), on by default via a `showQr` prop, so all three existing call sites (`campaigns/[slug]/page.tsx`, `CleanupEventDetail.tsx`, plus any future usage) get it automatically with no per-page changes. Verified end-to-end via a driven headless-browser test (open campaign page → dismiss instructions modal → click QR button → modal renders a scannable code pointing at the correct URL + working copy-link). Out of scope for this pass: `GroupStatsView.tsx`'s "Share" button shares a rasterized stat-card *image*, not a link — different UX, not touched.
- [x] **HIGH PRIORITY — Partner apply link missing from public Partners page** (noted 2026-08-09, done 2026-08-09) — added an "Own a business?" link to `/partners/apply` on the public Partners page header (`frontend/src/app/partners/page.tsx`).
- [x] **HIGH PRIORITY — Cleanup event check-in should award 5 points** (noted 2026-08-09, done 2026-08-09, tested 2026-08-09, prod migration applied 2026-08-10) — self check-in and organizer check-in both award points once per attendee per event via a new `cleanup_event_checkin` contribution type (migration `067_cleanup_event_checkin_points.sql`); value is admin-editable (`cleanup_event_checkin_value` in `game_settings`, default 5); UI shows upfront "Earn N points for checking in" messaging before check-in and "+N pts" next to the checked-in badge after, for both the attendee and the organizer's view of the attendee list. App header point badge now updates in real time on check-in bonus and on log-team-total credits (`refreshUserPoints` wired into `CleanupEventDetail.tsx`).
- [x] **Partner offer points-threshold notification + map affordability highlight** (built 2026-08-09, prod migrations applied 2026-08-10) — the `offer_eligible` notification + `AchievementModal.tsx` popup already existed end-to-end (migration `065_milestone_offer_no_push.sql`); new work this pass: (1) all milestone ladders (points/contribution-count/cleanup-bag/cleanup-pound) are now admin-editable via individual `game_settings` rows under a new `milestones` category instead of hardcoded PL/pgSQL arrays (migration `068_admin_editable_milestone_ladders.sql`); (2) Trash War map business markers now pulse/glow (`animate-affordable-pulse` in `globals.css`) and the click-through detail panel shows distinct "you have enough points!" messaging when the signed-in viewer can currently afford one of that business's active offers, computed per-request outside `getCampaignPageData`'s shared `unstable_cache` to avoid leaking one viewer's eligibility to others (`campaigns/[slug]/page.tsx`, `CampaignMap.tsx`). Not yet covered: a brand-new offer created after a user already qualifies won't retroactively notify (trigger only fires on `profiles` point changes, not `partner_offers` inserts) — flagged, not addressed.
- [ ] **HIGH PRIORITY — Tracked routes for group events** (scoped 2026-08-29) — let attendees track their route live (same background-tracking UI as individual route tracking, closeable window) directly from the group event page, plus a new "Routes & Photos" view on the event page showing every attendee's tracked route and photos together. Needs an explicit "Track my route" entry point on `CleanupEventDetail.tsx` (today only reachable via proximity auto-detection on the main map), a new `GET /cleanup-events/{id}/routes` endpoint (no schema changes — joins existing `contributions.cleanup_event_id` → `cleanups`), and a new event-scoped routes list page. Blocked on Track mode's own general-availability decision (currently `isSiteAdmin`-gated); only applies to `individual`-mode events, not `organizer_total`. → `group-event-route-tracking-scoping-2026-08-29.md`
- [ ] **Cleanup Routes (Phase 3)** — status **unclear/conflicting between docs**: `cleanup-routes-handoff.md` describes it as entirely not-started (new endpoint, `RoutePicker.tsx`, route detail page); `campaign-app-scope.md` describes "Cleanup routes (Beta)" as already shipped. **Reconcile which doc is current before resuming or re-scoping this.** → `cleanup-routes-handoff.md`, `campaign-app-scope.md`
- [ ] **Contractor bag tier** (~42-55 gal, ~10-13x a small bag) — needs new metrics column/migration, scoring multiplier, form/aggregation/admin/verification surfacing. → `trash-war-feedback-backlog.md` (item 7.5), `project_trash_war_deferred` memory
- [x] **Business user portal — redemption-history detail** (built 2026-08-13) — `GET /partners/businesses/{business_id}/redemptions` (business-admin gated, JOIN not LEFT JOIN on `partner_offers` so expired offers still show their title, paginated), new `RedemptionHistoryTable.tsx` with per-offer filter, wired as a tab in `PartnerDashboardClient.tsx`. → `dev-backlog-2026-07-24.md` (item 9), `glistening-mixing-bear.md` plan
- [x] **Group data portal — Phase 1 + CSV export** (built 2026-08-11) — deep-dive stats page at `/groups/[slug]/stats`: per-campaign aggregate + per-member contribution breakdown, interval-filterable, all members can view, admins can CSV-export (`GET /api/groups/{group_id}/stats`, `.../stats/export.csv`). → `group-data-portal-scoping-2026-08-11.md`
- [x] **Group data portal — Phase 2** (built 2026-08-11) — Excel export (`.../stats/export.xlsx`, `openpyxl`); shareable stat-card visualization (`ShareCard.tsx` rasterized client-side via `html-to-image`, shared/downloaded via new `shareImage()`/`downloadBlob()` in `share.ts`); map snapshot export (new deduped `GET /{group_id}/stats/geo-points` route + MapTiler Static Maps API composited into the same ShareCard/share pipeline). Historical import still deliberately deferred — least-important item per user, not scheduled. → `group-data-portal-scoping-2026-08-11.md`
- [x] **Business "radius of influence" view** (built 2026-08-13) — 3-tier selectable radius toggle (block/neighborhood/wide, server-side meters map) around a business location, `GET /partners/businesses/{business_id}/locations/{location_id}/radius-stats` using `ST_DWithin` against `contributions.location` (confirmed via `EXPLAIN ANALYZE` to use `contributions_location_idx`, not a sequential scan). Sparse-data risk solved via a citywide/campaign-wide comparison number shown alongside the local count, plus a `floor_state: "building"` for genuinely new locations/campaigns. New `BusinessRadiusView.tsx`, wired as a tab in `PartnerDashboardClient.tsx`. No ShareCard/map snapshot in v1 — deferred as a fast-follow. → `campaign-app-scope.md` (partner businesses section), `glistening-mixing-bear.md` plan

## Testing / QA (unexecuted)

- [ ] **Beta launch smoke test checklist** — signup/login/OAuth/profile/group admin/account settings/delete account/trash report/territory panel/hotspot/cleanup contribution/hex bloom — all unchecked. → `beta-launch-checklist.md`
- [ ] **External Model Imports** (Groups/Cleanups/Trash Reports canonical schema) — `beta-launch-checklist.md` lists as open/unresolved (API integration vs. migration vs. shared types package, undecided); **but `campaign-app-scope.md` shows this as done** — reconcile before treating as open. → `beta-launch-checklist.md`, `campaign-app-scope.md`
- [ ] **Deployment setup** — Railway vs. Cloud Run decision, Railway/Vercel project + env vars, CORS for prod domain, territory decay cron setup. → `beta-launch-checklist.md`
- [ ] **Staging environment** (scoped 2026-08-10, not started) — separate Supabase project + Vercel `staging` branch env vars + Railway `staging` environment, own R2 bucket; deliberately NOT another schema split (see `project_schema_split_todo` memory for why that failed before). Decided: dedicated `staging` git branch (PRs → `staging` → `master`), crons skipped for now. → `staging-environment-scoping-2026-08-10.md`
- [ ] **`game_settings` overhaul test plan** (migrations 056-057) — entirely unexecuted: settings page behaviors, hotspot/report-value tuning, recompute-all-balances fix verification (incl. bind-param truncation regression), spendable-points toggle preview fix, migration sanity checks, prod recompute-impact preview before any real recompute run. → `testing-strategy-game-settings-overhaul.md`
- [ ] **Cleanup Routes verification plan** (defined, not executed) — multi-zip crossing, valid/invalid zip rejection, individual/group/group-event paths, point-submission regression. → `cleanup-routes-handoff.md`

## Auth

- [ ] **HIGH PRIORITY — Lock down FastAPI/Railway backend auth** (noted 2026-08-12) — discovered while fixing a prod bug where the "Download my data" link pointed straight at the Railway backend URL (cross-origin `<a download>` was ignored by the browser, causing a raw navigation to a backend error page). Root cause of that specific bug is fixed (`frontend/src/app/api/account/export/route.ts` now proxies same-origin, resolving the user id from the session). But it surfaced a broader pattern: backend routes like `GET /api/account/{user_id}/export` and `DELETE /api/account/{user_id}` trust whatever `user_id` is in the path with **zero auth check of their own** — they rely entirely on the Next.js layer to only ever call them with the verified caller's own id. There's currently no defense-in-depth: if the Railway URL structure is ever hit directly (leaked link, guessed UUID, bug in a route handler that forwards the wrong id), it's a live IDOR/PII exposure. Needs a real fix — e.g. verifying a forwarded Supabase session/JWT on the backend itself, or a shared secret between the Next.js layer and FastAPI — not just trusting the frontend to always gate correctly.
- [ ] **Additional sign-in methods** (post-beta, not started): GitHub, Apple (required for iOS App Store once mobile ships), Discord, Twitter/X, Passkeys/magic link. → `campaign-app-scope.md`

## Post-MVP / big future bets (not started)

- [ ] React Native app + shared auth/API layer
- [ ] Collective Action Fund (pooled donation + voting) — needs legal review first
- [ ] Campaign creation tools for verified groups
- [ ] Weather API integration for dynamic events
- [ ] Advanced moderation layer for user-generated content
→ all in `campaign-app-scope.md`
- [ ] **Payment platform integration (Stripe or similar)** (noted 2026-08-11) — not started, no payment provider anywhere in the codebase today. Two known use cases so far: (1) **payouts** — prize money for challenge winners (e.g. "most cleaned in a week wins $100"), which needs real payout rails (Stripe Connect or similar), identity/KYC on payout recipients, and a challenge/leaderboard-driven trigger for who's owed what; (2) **charges** — later, billing partner businesses to list on the platform and/or for premium features, which needs subscription/invoicing support, not just one-off charges. These two use cases pull toward different Stripe products (Connect for payouts vs. Billing/Checkout for charges) and may end up as two separate integrations sharing one Stripe account rather than a single unified build. Also needs: legal/tax review before real money moves either direction (this is likely the bigger blocker than the code), decision on which use case to build first. Not scoped in detail yet. → `campaign-app-scope.md`
- [ ] **Clean data deletion + downstream reset** (scoped 2026-08-10) — the new admin Moderation tab (`content_flags` queue, migration `072_content_flags_resolution.sql`) can hide an individual reported photo, but there's still no way to fully delete a piece of submitted content (a cleanup event, a contribution) and have everything it fed into unwind cleanly: territory claims, geographic stats/leaderboard totals, and points/spendable_points all need a correct recompute or rollback path, not just an orphaned row deletion. `wipe_cleanup_event` (`admin.py`) reverses cleanup-logging side effects for a still-existing cleanup, but nothing does a full delete-and-reset. Worth designing before it's needed for real (e.g. a bad-faith or duplicate cleanup submission with points/territory already propagated).
- [ ] **Community Goals** (scoped 2026-08-07) — visible collective progress targets (platform-wide, per-campaign, per-group) to drive individual + group adoption; self-serve group goal creation is the piece most worth building for "use this as our tracking platform." Deliberately deprioritized behind mobile/Capacitor app store push. → `community-goals-scoping-2026-08-07.md`

## Explicitly decided out of scope (recorded so it doesn't get re-litigated)

- Retroactive point recalibration when bag/pound/multiplier constants change — decided against 2026-08-03. → `dev-backlog-2026-07-24.md` (item 8 Part B)
- Business self-serve management of additional business admins — revisit only if a business actually needs it. → `dev-backlog-2026-07-24.md` (item 9)
- Per-notification-type user preferences, rich push (images/actions), notification digests/batching. → `push-notifications-scoping-2026-08-06.md`

## Reference-only (setup/how-to guides, no independent backlog)

- `capacitor-testing-guide.md` — how to run against local dev vs. prod.
- `ios-setup-2026-08-06.md` — iOS scaffolding steps (tracked as tasks under Mobile section above).
- `new-machine-setup-2026-08-06.md` — moving to a new dev machine.
- `mobile-first-design-system.md` — design principles reference; its one backlog pointer is under Mobile above.
