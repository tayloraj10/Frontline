{
  "name": "capacitor-background-geolocation-frontline",
  "version": "0.1.0",
  "private": true,
  "description": "Custom Capacitor plugin: iOS background GPS route tracking for Frontline's live cleanup route feature.",
  "main": "dist/esm/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "dist/",
    "ios/Sources",
    "Package.swift",
    "BackgroundGeolocationFrontline.podspec"
  ],
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf ./dist",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@capacitor/core": "^8.0.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@capacitor/core": ">=8.0.0"
  },
  "capacitor": {
    "ios": {
      "src": "ios"
    }
  }
}
