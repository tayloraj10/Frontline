# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
.env.local
.env*.local
!.env.example

# Build outputs
.next/
out/
dist/
build/

# Misc
.DS_Store
*.log
.vercel

# Large geo data files — generated locally, not committed
backend/data/

# Local-only dev convenience script (pulls prod data into local Supabase) — never committed,
# even though it takes credentials via env var rather than storing them, to keep it firmly
# out of anyone's PR history.
backend/scripts/pull_prod_snapshot.sh

